# Аудит frontend-зависимостей

Дата проверки: 11 августа 2026.

React Router обновлён до `7.18.2` после регрессионной проверки используемых SPA API.
Уязвимость открытого перенаправления в старой ветке устранена.

Результат `npm audit --omit=dev`:

- production-уязвимости: 0;
- high: 0;
- critical: 0.

Дополнительно обновлены транзитивные `nanoid` и `postcss`. Полный `npm audit`,
включая инструменты сборки, также сообщает `0 vulnerabilities`. CI должен продолжать
выполнять аудит и production-сборку при каждом изменении.
